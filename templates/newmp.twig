{% extends "base.twig" %}
{% block title %}Nouveau message privé{% endblock %}
{% block content %}
<form action="mp.php" method="post">
<div class="box">
<div class="box-header">
<h3 class="box-title">Nouveau message privé</h3>
</div>
{% if friends is not empty %}
<div class="box-body">
	<div class="form-group">
		<label for="titre">Titre <small>(facultatif)</small></label>
		<input class="form-control" name="titre" placeholder="Entrez un titre ici...">
	</div>
	<div class="form-group">
        <label>Destinataires</label>
        <select aria-hidden="true" tabindex="-1" class="form-control select2 select2-hidden-accessible" style="width: 100%;" multiple="multiple" name="dest[]">
          {% for friend in friends %}
          	<option value="{{ friend.id }}">{{ friend.prenom }} {{ friend.nom }}</option>
          {% endfor %}
        </select>
    </div>
	<div class="form-group">
		<label for="message">Message</label>
		<textarea name="message" class="form-control" placeholder="Entrez votre message ici" rows="3"></textarea>
	</div>
</div>
<div class="box-footer">
<button class="btn btn-primary" type="submit">Envoyer !</button>
</div>
{% else %}
	<div class="callout callout-danger">
				<h4>Aucun ami</h4>
				<p>Hélas, vous n'avez (encore ?) aucun ami. Vous ne pouvez par conséquent pas encore envoyer de message privé.</p>
			</div>
{% endif %}
</form>
{% endblock %}